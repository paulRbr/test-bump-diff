overlay: 1.0.0
info:
  title: Overlay to customise Bump.sh API
  version: 0.0.1
actions:
  - target: "$.info"
    update:
      description: |
        Hi folks,

        This is in english

        😉😉😉
  - target: '$.tags[?(@.name=="Branches")]'
    update:
      description: |
        This is in english
  - target: '$.components.schemas.Hub.properties.name'
    remove: true
